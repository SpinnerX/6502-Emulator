#pragma once
#include <chrono>
#include <algorithm>
#include <cstring>

/**
 * 
 * 
 * Clock
 * - Acts as the timer
 * - That allows the pace of specific operations in the emulated processor to be controlled
 * - CPU Procesors operate according to its clock cycles, with one operation typically carried out per cycle.
 * - 
 * 
 * 
*/

struct Clock{

    void tick(){}



};